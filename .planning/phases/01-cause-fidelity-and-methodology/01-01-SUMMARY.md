---
phase: 1
plan: 01-01
subsystem: cause-data
tags:
  - ihme
  - persona
  - data-pipeline
key-files:
  - scripts/build-causes.mjs
  - data/causes.json
  - public/persona.js
  - README.md
  - ACCURACY_STEPS.md
metrics:
  causes: 140
  countries: 0
  bands: 9
---

# Plan 01-01 Summary: Build and verify real cause data

## One-liner

Generated and verified an IHME-derived global, sex-specific `data/causes.json` compatible with the existing persona pipeline.

## Completed Work

- Updated `scripts/build-causes.mjs` to support the current IHME GBD export shape: `location_name=Global`, `age_name=All ages`, `sex_name=Male/Female`.
- Added output coverage metadata:
  - `location: global`
  - `age: all_ages_repeated_across_bands`
  - `sex: male_female`
- Generated `data/causes.json` from the local IHME GBD 2023 CSV export.
- Confirmed the output has 140 cause labels and nine male/female band cells for compatibility with `public/persona.js`.
- Verified `public/persona.js` consumes the generated cause data through `initPersona()` and `makePersona()`.
- Preserved fallback behavior: missing cause data falls back to sample persona data or built-in illustrative cause tables.

## Commits

| Task                   | Commit       | Description                                                                                                        |
| ---------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------ |
| Cause builder and JSON | working tree | Changes are present but not committed because `commit_docs=false` and the repository already has uncommitted work. |

## Verification

Passed:

```bash
npm run build:causes -- --force
node --check scripts/build-causes.mjs
node -e "<JSON shape check>"
node --input-type=module -e "<persona smoke>"
```

Observed generated-data shape:

```json
{
  "year": 2023,
  "coverage": {
    "location": "global",
    "age": "all_ages_repeated_across_bands",
    "sex": "male_female"
  },
  "causes": 140,
  "countries": 0,
  "mBands": 9,
  "fBands": 9
}
```

## Deviations

- The original roadmap language expected full IHME-derived cause distributions when available. The accepted Phase 1 boundary treats the current Global + All ages export as the MVP cause layer and defers per-country/age cause precision.
- No commit was created during execution.

## Self-Check

PASSED. `data/causes.json` exists, is regenerated by the build command, and is loadable by the persona pipeline.
